---
meta:
  environment: jvm
  toolchain:
    name: maven
    version: 17
build:
  artifacts:
    build/libs/*.jar: /
run:
  jarName: car-bot-service-1.0-SNAPSHOT.jar
  persistenceMount: /data
  containerPort: 80
